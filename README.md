# -Java-based-CLI
A Java-based CLI application for managing students, teachers, and  courses. Built with OOP, multi-package structure, and in-memory  database. Features a command-line menu for CRUD operations.
